# Ụgwọ app — Expo public environment
# Copy to .env — values are baked into the JS bundle at build time.

# The API base URL (no trailing slash).
# Dev:        http://localhost:3001  (or http://<your-LAN-IP>:3001 for devices)
# Production: https://ugwo.nippysky.com
EXPO_PUBLIC_API_URL=https://ugwo.nippysky.com
